const Form = () => {
  return (
    <div>
      <button
        onClick={() => {
          alert("clicked");
        }}
      >
        Submit
      </button>
    </div>
  );
};

export default Form;
